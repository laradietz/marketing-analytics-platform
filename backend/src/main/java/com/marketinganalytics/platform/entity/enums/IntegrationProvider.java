package com.marketinganalytics.platform.entity.enums;

/** External ad platform an account can be connected to for automatic metric sync. */
public enum IntegrationProvider {
    META_ADS,
    GOOGLE_ADS,
    TIKTOK_ADS
}
